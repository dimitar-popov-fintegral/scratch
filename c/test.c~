#include <stdio.h>

int main(){

  int MAX = 10;
  int arr1[MAX];
  int * p = arr1;
  int n = 7;

  //"allocate" some memory first
  p=p+2;

  //check if we can "allocate" more
  if(arr1 + p + n <= MAX)
    printf("Yes!");

  return 0;

}
